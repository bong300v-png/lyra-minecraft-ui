LYRA — SRC NOTES
================
6 trang: Landing, Store, Play, Login, Forums, Terms (.dc.html)

Lưu ý khi chạy:
- Các file này là Design Component (.dc.html), cần file runtime `support.js` đặt cùng thư mục (file này nằm trong project gốc, hơi nặng nên không đẩy qua kênh chat được — tải project zip từ app rồi lấy ra).
- Logo + icon load từ CDN (cloudfront), link đã nhúng sẵn trong HTML.
- Font: Unbounded + Space Grotesk (Google Fonts, load tự động).

Repo tham chiếu: github.com/bong300v-png/lyra-minecraft-ui (branch main)
